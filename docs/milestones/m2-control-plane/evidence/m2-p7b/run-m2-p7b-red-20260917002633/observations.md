# M2-P7B Behavioral RED — oracle #001 đã hiệu chỉnh

- Review checkpoint `423022f0f887e2301008298fbf6a707b6a3f41e6` không chấp nhận ứng viên RED `run-m2-p7b-red-20260917000606`: #001 seed event cũ rồi subscribe không cursor nhưng đòi phát lại event đó, trái baseline MAX đã commit của initial subscribe. Run cũ giữ nguyên làm lịch sử bị từ chối.
- Source/test pin `c35571fb334dc7b842ee23378f041e74f1caa277`; oracle SHA-256 `d83d0f2d808f1b0067d5288ea131ded24d8fc46a16462b5254fd4167f7425738`. Exact collect năm identity #000–#004; full RED `5 failed / 0 passed / 0 errors / 0 skipped`.
- #000 `UPSTREAM_INVARIANT_RED`: ba lần độc lập đều thấy T1 append N=1 rồi giữ transaction, T2 append M=2 commit trước, reader advance đến 2, T1 commit N muộn; replay `>2` bỏ N. Dùng PostgreSQL P3 repository thật, hai connection và transaction, không sửa `append()`.
- #001 `VALID_CAPABILITY_RED_PRESENTER`: seed boundary C=1 và event tiếp N=2; HTTPS request thật dùng `?cursor=1` để yêu cầu replay exclusive. Client `httpx.Client(verify=SSLContext)` với CA của transient loopback certificate, session P7A hợp lệ lưu server-side, route thật của cùng `create_app()`. Test vào `client.stream`, xác minh HTTP 500 safe, `technical_detail_ref` và bản ghi durable `NotImplementedError` chứa `P7B_SSE_FRAMING_NOT_IMPLEMENTED`, rồi fail đúng kỳ vọng HTTP 200. GREEN oracle sẽ đọc tới một data frame hoàn chỉnh kết thúc bởi dòng rỗng, kiểm N chứ không phát lại C, SSE headers, compact JSON/cursor rồi thoát context để chủ động disconnect; không đòi server đóng ordinary SSE.
- #002 `VALID_CAPABILITY_RED_REPLAY`: bốn durable rows rồi chạm `SseStreamService.replay_after` NII. #003 `VALID_CAPABILITY_RED_CLASSIFICATION`: watermark thật rồi chạm `SseStreamService.classify_cursor` NII. #004 `VALID_CAPABILITY_RED_WORKSPACE_READER`: A/B durable rows, session A, unauthenticated 403, rồi chạm `PostgresSseReader.fetch_workspace_page` NII. Ba oracle này không đổi ngữ nghĩa.
- PostgreSQL 18.6, CREATEDB=true, connection PASS và disposable DB orphan=0 trước/sau. P7A oracle 4/4, hardening 36/36, P3 11/11, architecture 6/6. Bổ sung P6 4/4, P5B 5/5, P5A 5/5, P4 9/9, P2 11/11, P1 11/11, P0 33/33. `M1=NOT_RUN`, không có PASS claim.
- P0 regression tạo untracked `src/controlplane/build`; đã chuyển an toàn sang `.local-tools/temp/p7b-build-backup-20260917002633` để giữ khả năng khôi phục và worktree sạch. Không sửa production P7B seam, P3 writer, accepted P7A oracle/evidence, migration 0001–0007 hoặc dependency pin; không tạo 0008.
- Bộ điều phối evidence có một lần preflight dừng sau prerequisite PASS do hàm PowerShell trả thêm dòng tiến độ vào exit code. Không có test nào chạy ở lần đó; prerequisite và toàn bộ run đã được thực thi lại, ghi provenance đầy đủ trong `commands.jsonl`. Đây không phải RED hay PostgreSQL blocker.
- Ruff và `uv lock --check` PASS; secret scan scoped source/docs/evidence CLEAN/0. Evidence command records chỉ chứa argv đã thực thi, source SHA, exit code, thời điểm và artifact; M1 có `executed=false`/`NOT_RUN`, không có argv giả.

Chỉ `M2-P7B_BEHAVIORAL_RED_READY_FOR_REVIEW`; GREEN/ordering fence vẫn `LOCKED`.
