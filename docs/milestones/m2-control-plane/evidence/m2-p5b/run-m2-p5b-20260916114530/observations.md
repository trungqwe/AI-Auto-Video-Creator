# Quan sát corrective M2-P5B RED

Exact five RED đúng capability; location tests có FK-parent seed chỉ khi production table tồn tại. Package path là `storage_meta`. Historical run giữ nguyên; implementation và `0005` vẫn khóa.
