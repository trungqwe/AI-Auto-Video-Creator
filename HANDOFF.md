# HANDOFF

- Đã commit và push oracle correction `4990eb5b37057dddce66249970a0d482c854c369` (`test(m2-p5a): correct postgres and cas closure oracles`). RED mới được ghi bất biến tại `docs/milestones/m2-control-plane/evidence/m2-p5a/run-m2-p5a-20260916000000/`; hash DAG PASS. RED có đúng 5 collect, 3 failure hành vi (CAS stale-current, side effect shared ledger, global psycopg patch), 2 pass, không error/skip.
- Worktree hiện có source correction **chưa commit**: bỏ global `psycopg.Connection.execute` patch, đưa adapter PostgreSQL P5A sang `infrastructure/db/config_security`, sửa stale CAS trả revision hiện hành, và bỏ trigger/function tác động `cp_schema_migrations` khỏi migration 0004.
- P5A corrected exact suite: `5 passed`. P4/P2/P1 pass trong regression chung; P3-001 fail sau source correction: rollback trực tiếp `0003` chỉ xóa version 3, nhưng migration runner đã ghi 0004 nên ledger thực tế là `(1,2,4)`; historical P3 test yêu cầu `(1,2)`.
- BLOCKED: không thể vừa giữ `0004` không chỉnh shared migration ledger, không sửa `MigrationRunner`, không sửa test/source P1--P4, vừa đạt P3 11/11. Không có source/evidence implementation checkpoint mới; không chạy P0/M1, không bắt đầu P5B/P6+/M3/Module A.
- Cần quyết định tiếp: cho phép thay đổi historical P3 oracle, hoặc nêu cơ chế runner/ledger được phép thay đổi. Không amend/reset/force; không commit worktree source khi blocker chưa được giải quyết.
