# HANDOFF

- Đã quyết định: M1, M2-P0, M2-P1 và M2-P2 là `ACCEPTED / CLOSED`. M2-P3 ở `M2-P3_RED_READY_FOR_REVIEW`: exact 11 RED hợp lệ đã được chứng kiến trên PostgreSQL 18.6; chờ independent audit trước implementation. M2-P4..P7, M3 và Module A vẫn bị khóa.
- P3 plan khóa đúng 11 mandatory identities trong `implementation-plan.md`: migration/schema `0003`, UoW outbox atomicity, at-least-once crash/re-dispatch, dedupe/concurrency/checkpoint atomicity, schema/epoch quarantine, aggregate ordering, operation stream cursor/safe projection. Correction planning mới nhất trace OperationStream tới `CT-API-008`, persistence giữ full `MessageEnvelope` + `DomainEvent`, aggregate ordering là non-unique, quarantine scope theo consumer và ordering/reconcile state là durable/explicit.
- Future P3 persistence chỉ qua active P1 UoW: application/domain không SQL/psycopg/tên bảng; adapters được phép tại `infrastructure/db/outbox/**` và `infrastructure/db/projections/**`, không pool/commit/rollback/transaction ẩn. Có đúng 11 test harness và importable structural stubs chỉ ném `NotImplementedError`; chưa có migration, functional source, collection hay runtime RED P3.
- Runtime future P3: Python 3.13.15, psycopg 3.3.5, psycopg-pool 3.3.1, PostgreSQL 18.6, `CREATEDB=true`, `psycopg_pool.ConnectionPool`; disposable DB `^m2_p3_test_[0-9a-f]+$`, orphan=0.
- Future closure giữ P3 11/11; frozen P2 11/11, P1 11/11, P0 33/33 bao gồm architecture 6/6, M1 93/93/0 skipped; P3 evidence profile/synthesizer fail-closed.
- Tệp cần đọc tiếp: `docs/milestones/m2-control-plane/spec.md`, `implementation-plan.md`, `docs/09-contracts/02-domain-events.md`, `docs/12-pre-code-checklist.md`.
- Điểm tiếp tục: khôi phục đúng runtime `psycopg_pool==3.3.1`, rồi chạy prerequisite → collect exact 11 → full PostgreSQL RED và phân loại. Không triển khai P3 trước independent review evidence.
