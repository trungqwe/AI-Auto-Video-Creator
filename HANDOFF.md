# HANDOFF

- Trạng thái có thẩm quyền: `M2-P1..P7A_ACCEPTED_CLOSED; M2-P7B_AUTHORITY_ACCEPTED; M2-P7B_BEHAVIORAL_RED_ACCEPTED; M2-P7B_IMPLEMENTATION_AUTHORIZED; M2-P7B_INDEX_AUTHORITY_DELTA_ACCEPTED`. P7B chưa accepted/closed; P8/P9 khóa; M3/Phân hệ A `NOT AUTHORIZED`.
- Independent review chấp nhận P7B Behavioral RED source/test `c35571fb334dc7b842ee23378f041e74f1caa277`, oracle SHA-256 `d83d0f2d808f1b0067d5288ea131ded24d8fc46a16462b5254fd4167f7425738`, evidence `run-m2-p7b-red-20260917002633`. Oracle và evidence này bất biến. Run `run-m2-p7b-red-20260917000606` là lịch sử bị từ chối, cũng bất biến.
- Quyền triển khai chỉ áp dụng P7B, ngoại lệ P3 đúng thân hàm `PostgresOperationStreamRepository.append()` và hai tệp migration `0008_operation_stream_workspace_cursor_index.sql`/`.rollback.sql`: single-statement per-workspace transaction fence → allocation → INSERT; index B-tree thường `(workspace_id, stream_event_id)` và rollback chỉ drop index. Không sửa P3 code khác, migrations `0001..0007`, `MigrationRunner` hoặc tạo `0009`. Future GREEN H01–H38 count=38, P3 11/11 và M1 93/93 là hard gates.
- Gate `EXPLAIN (ANALYZE, BUFFERS)` trên PostgreSQL 18.6 với 100.000 stream rows thuộc 98 workspace khác: truy vấn `WHERE workspace_id = A AND stream_event_id > 0 ORDER BY stream_event_id LIMIT 100` quét tuần tự 100.000 rows, 1.725 shared buffers; resume từ 50.000 quét PK rồi loại 50.000 rows, 1.005 buffers. PK-only không cho bounded read theo workspace. Thử nghiệm one-statement fence làm oracle P7B #000 và P3 #010/#011 xanh 3/3, nhưng thay đổi P3 đã được hoàn nguyên, không commit; không có P7B source/GREEN mới.
- Independent review checkpoint `aed557f5269f0af7641197d9b662b2dcff5e8d65` đã chấp nhận blocker và cấp authority delta cho đúng index/migration nêu trên. Điểm tiếp tục: tái lập pre-index EXPLAIN trong DB disposable, kiểm chứng forward/targeted rollback/reapply `[1..8] → [1..7] → [1..8]`, post-index H18 rồi tiếp tục fence/SSE/H01–H38. Lưu artifacts chỉ vào GREEN run mới; accepted RED bất biến. P8/P9/M3/Phân hệ A chưa được phép.
