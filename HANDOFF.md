# HANDOFF

- Trạng thái có thẩm quyền: `M2-P1..P7A_ACCEPTED_CLOSED; M2-P7B_AUTHORITY_READY_FOR_REVIEW; M2-P7B_BEHAVIORAL_RED_LOCKED; M2-P7B_IMPLEMENTATION_LOCKED`. P8/P9 khóa; M3/Phân hệ A `NOT AUTHORIZED`.
- P7A immutable source/tooling `6c3a52bde905ee5e71e12334da1873ed20f5c5db`, accepted GREEN `run-m2-p7a-green-20260916224033`, oracle SHA-256 `63151da21b07c3dd92c5b4a7acc0d4f952f188ee2425a52c9d3ac8d34eb61035`. Accepted RED, rejected GREEN cũ, accepted GREEN mới và P1–P6 source/evidence giữ byte-exact; không chạy lại.
- Independent review checkpoint `a60471af2ab03978e6a88657dbec62a43b82da0c` chọn future P7B hướng B: per-workspace `pg_advisory_xact_lock` trên caller-owned transaction **trước** `stream_event_id` allocation; exact accepted-P3 exception duy nhất `src/controlplane/infrastructure/db/projections/postgres.py:PostgresOperationStreamRepository.append()`. Chưa sửa source; P3 11/11 và các tệp/evidence accepted khác read-only.
- P7B plan docs-only khóa watermark `minimum_available_cursor` = oldest resumable cursor C: `cursor == C` replay `> C`, `< C` resync rồi close; không cần row C còn tồn tại, không suy từ physical MIN hoặc sequence gap. Initial subscribe dùng committed visible MAX của authenticated workspace. RED tương lai đúng 5 case, trong đó #000 `UPSTREAM_INVARIANT_RED` trên PostgreSQL 18.6 hai writer thật; 001–004 giữ capability seams. H37 là proof GREEN độc lập nhiều iteration. Static profile sẽ fail nếu có writer ngoài approved append hoặc fence sau allocation.
- Future reader vẫn có conditional STOP gate về PK-only query plan thiếu `(workspace_id, stream_event_id)` index: đo realistic multi-workspace `EXPLAIN`, nếu không đạt thì xin authority delta; không tự tạo `0008`. Evidence P7B chỉ ghi command thực chạy; policy/skip ghi `executed=false`; không DSN/token.
- Đọc tiếp `docs/12-pre-code-checklist.md`, mục P7B trong `docs/milestones/m2-control-plane/implementation-plan.md`, schema `0003_outbox_and_projections.sql`, P3 writer và accepted P7A status. Điểm tiếp tục: independent review của authority correction này; chưa tạo P7B RED/code/evidence.
