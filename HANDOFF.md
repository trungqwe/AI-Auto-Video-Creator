# HANDOFF

- Đã quyết định: Toàn bộ các vấn đề từ đợt tái kiểm toán độc lập M1 R5.1 trên GitHub HEAD commit `ed0fc83` (1 BLOCKER secret ownership, 1 MAJOR semantic validation) đã được giải quyết triệt để và kiểm chứng thực nghiệm: (1) Broker-Owned OAuth Provisioning: Desktop orchestrator (`drive_live_probe.py`) loại bỏ 100% import `DPAPISecureVault` và `InstalledAppFlow`, không gọi `vault.get_account()`; Broker subprocess là tiến trình duy nhất mở và giải mã DPAPI vault; Desktop ủy quyền provisioning qua HTTP IPC `POST /api/provision` (hoặc CLI flag `--provision-credentials`) và nhận ephemeral access token ngắn hạn; thực nghiệm live probe E3 thành công trên Google Drive thật (`broker_pid=23076`, `desktop_pid=23768`, upload 64 bytes ID `1R6D6B...R5Ux`, đối soát SHA-256 khớp 100%, dọn dẹp file test); xuất `drive_e3_evidence.json` đầy đủ các trường machine-readable và limitation: "DPAPI proof runs under one Windows user; OS-account isolation between cloud host and desktop belongs to later deployment validation" (thêm test TST-M1-P3-015); (2) P6 Semantic Capability Validation: Siết chặt validator P6 với 10 điều kiện bắt buộc; negative tests TST-M1-P6-010 kiểm tra từng trường sai/thiếu đều fail-closed và chặn cấp `READY_FOR_USER_CHECKPOINT`; (3) Chuẩn hóa tài liệu: `audit-r5.md` bổ sung mục `6. R5.1 Addendum`, sửa schema vault thành `version/encryption/encrypted/ciphertext/updated_at`; (4) Chứng kiến RED thật trước code cho 2 test mới (`red-r5-1-stdout.txt`); toàn bộ test suite M1 đạt **93/93 passed, 0 skipped** (coverage 83%), 85 artifacts trong `manifest.json` toàn vẹn SHA-256 100%, 0 secret rò rỉ; Gate engine M1 kết luận `READY_FOR_USER_CHECKPOINT`.
- Chưa quyết định: Phê duyệt User Checkpoint cho Milestone M1 từ Người dùng và quyết định cho phép mở Milestone M2. Toàn bộ Milestone M2, M3 và Phân hệ A tiếp tục bị khóa chặt (`NOT AUTHORIZED`) cho đến khi có User Checkpoint phê duyệt M1.
- Tệp cần đọc tiếp: `docs/milestones/m1-proof/audit-r5.md` (mục 6. R5.1 Addendum), `docs/12-pre-code-checklist.md`, `docs/11-roadmap.md`.
- Điểm tiếp tục: Báo cáo Người dùng toàn bộ kết quả khắc phục R5.1 và dừng chờ User Checkpoint phê duyệt Milestone M1.

