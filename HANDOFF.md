# HANDOFF

- Đã quyết định: Toàn bộ các vấn đề từ đợt kiểm toán độc lập R5 trên GitHub HEAD commit `9167072` (2 BLOCKER, 1 MAJOR) đã được giải quyết triệt để và xác thực thực nghiệm: (1) R5-01: Broker Subprocess OS Isolation (`subprocess.Popen`), Desktop client chỉ biết URL và giao tiếp REST IPC, `broker_pid != desktop_pid`, fail-closed khi broker bị kill; thực nghiệm live probe E3 thành công trên Google Drive thật qua broker subprocess (`broker_pid=49052`, `desktop_pid=20320`, resumable upload 64 bytes ID `1zLi2d...HsQD`, SHA-256 đối soát khớp 100%, dọn dẹp file test); xuất `drive_e3_evidence.json` có `process_isolated: true`; (2) R5-02: Kho bảo mật Windows DPAPI native (`CryptProtectData`/`CryptUnprotectData`), vault trên đĩa (`~/.cloud_token_broker/vault.json`) chỉ chứa base64 ciphertext, cam kết 0 byte plaintext token trên đĩa máy trạm; (3) R5-03: Fail-closed dynamic compatibility matrix, xóa bỏ 100% logic fallback gán giá trị mặc định, thiếu/lỗi thì `observed=None`, `result="FAIL"`, `overall_result="FAIL"`; (4) R5-04: Nâng cấp validator P6 kiểm tra sâu cấu trúc ngữ nghĩa machine-readable (P3 E3: `process_isolated==true`, `broker_pid != desktop_pid`, DPAPI storage verified; P5: mọi runtime `result=="PASS"`, không null/FAIL/unknown) trước khi cấp `E3` và `READY_FOR_USER_CHECKPOINT`; (5) R5-05: Chứng kiến RED thật cho 4 tests R5 trước implementation (`red-r5-stdout.txt`); sau correction toàn bộ test suite M1 đạt **91/91 passed, 0 skipped** (coverage 85%), 84 artifacts trong `manifest.json` toàn vẹn SHA-256, 0 secret rò rỉ; Báo cáo Kiểm toán Exit Gate `docs/milestones/m1-proof/audit-r5.md` kết luận READY_FOR_USER_CHECKPOINT.
- Chưa quyết định: Phê duyệt User Checkpoint cho Milestone M1 từ Người dùng và quyết định cho phép mở Milestone M2. Toàn bộ Milestone M2, M3 và Phân hệ A tiếp tục bị khóa chặt (`NOT AUTHORIZED`) cho đến khi có User Checkpoint phê duyệt M1.
- Tệp cần đọc tiếp: `docs/milestones/m1-proof/audit-r5.md`, `docs/12-pre-code-checklist.md`, `docs/11-roadmap.md`.
- Điểm tiếp tục: Báo cáo Người dùng toàn bộ kết quả khắc phục R5 và dừng chờ User Checkpoint phê duyệt Milestone M1.
