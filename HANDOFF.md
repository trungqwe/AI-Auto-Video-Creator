# HANDOFF

- Trạng thái có thẩm quyền: `M2-P1..P7A_ACCEPTED_CLOSED; M2-P7B_AUTHORITY_ACCEPTED; M2-P7B_BEHAVIORAL_RED_READY_FOR_REVIEW; M2-P7B_IMPLEMENTATION_LOCKED`. P8/P9 khóa; M3/Phân hệ A `NOT AUTHORIZED`.
- P7A immutable source/tooling `6c3a52bde905ee5e71e12334da1873ed20f5c5db`, accepted GREEN `run-m2-p7a-green-20260916224033`, oracle SHA-256 `63151da21b07c3dd92c5b4a7acc0d4f952f188ee2425a52c9d3ac8d34eb61035`. Accepted RED, rejected GREEN cũ, accepted GREEN mới và P1–P6 source/evidence giữ byte-exact; không chạy lại.
- Review checkpoint `25e7f2488d8298be08816bd2147315b4677eb442` giữ hướng B nhưng phát hiện accepted P3 `DisposableDatabase.connect()` dùng `autocommit=True`; #011 gọi `_stream(connection).append()` trực tiếp, #010 dùng P1 UoW. Future exact P3 exception vẫn chỉ `src/controlplane/infrastructure/db/projections/postgres.py:PostgresOperationStreamRepository.append()`, nhưng **rút lại two-statement fence**. Authority draft nay khóa **một PostgreSQL statement**: materialized ordering-fence CTE → dependent materialized explicit `nextval` CTE → INSERT/RETURNING. Không adapter-owned transaction/commit/rollback. Intended CTE shape chưa là runtime proof; H37/H38 phải xác minh lock trước allocation ở cả autocommit/UoW.
- Future RED vẫn đúng năm identity #000–#004; #000 chạy pre-fix accepted P3 writer thật, expected `UPSTREAM_INVARIANT_RED`, phải witnessed/accepted trước fence. Future GREEN H01–H38 count=38; H38 chứng minh P3 autocommit/UoW compatibility riêng H37. P3 accepted regression nguyên tệp 11/11, đặc biệt #010/#011, 0 fail/error/skip; không sửa accepted tests.
- Watermark giữ `minimum_available_cursor` = oldest resumable cursor C (`==C` replay `>C`, `<C` resync rồi close); index `(workspace_id, stream_event_id)` còn conditional STOP gate. Evidence P7B chỉ ghi external commands thật, policy/skip `executed=false`; không DSN/token. Không sửa P3/P7A source/test/evidence hoặc tạo `0008` tại checkpoint docs-only.
- Đọc tiếp `docs/12-pre-code-checklist.md`, mục P7B trong `docs/milestones/m2-control-plane/implementation-plan.md`, evidence `run-m2-p7b-red-20260917000606`. Source/test/seam `7fa928b1f715a66b0d5a579c2530884598ed482d`: exact 5 collected/5 failed đúng seam; #000 tái hiện 3 lần T1 N=1 commit muộn sau T2 M=2. P7A 4/4, hardening 36/36, P3 11/11, architecture 6/6; orphan=0, secret CLEAN/0. Chờ independent review Behavioral RED; GREEN và P3 ordering fence vẫn khóa.
