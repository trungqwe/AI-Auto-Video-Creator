"""Tracked production migration source directory for M2-P1 sandbox fixtures.

SQL migrations are deliberately absent until the separately authorized
implementation phase.  Fault tests copy this directory before modifying only
their temporary sandbox.
"""
